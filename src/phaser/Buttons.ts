// level?로 바꿀까.

export default class Buttons extends Phaser.Scene {
  constructor() {
    super({
      key: "Buttons",
      active: true,
    });
  }

  preload() {}

  create() {}
  update() {}
}
